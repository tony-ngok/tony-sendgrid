# tony-sendgrid

这是一个 Sendgrid（Email 订阅与传送系统）的练习专案。

## 如何执行

1. 确保已经安装 Node.js 24
2. 新增一个`.env`（环境变数）档案，按照`.env.example`的格式填写所需环境变数
3. 执行`npm install`，完成后执行`npm run build`
4. 执行`npm run dev`启动，然后在浏览器中打开`localhost:3002`